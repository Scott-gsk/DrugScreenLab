# DrugScreenLab

DrugScreenLab is an agent-first, reproducible research repository for AI-driven drug screening.

The repository starts with no active model, baseline, champion, or experiment. Dataset assets are registered separately from models and experiments.

Read `AGENTS.md`, `ARCHITECTURE.md`, and `data/README.md` before contributing.
