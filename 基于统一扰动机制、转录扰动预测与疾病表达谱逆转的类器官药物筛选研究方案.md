# 基于统一扰动机制、转录扰动预测与疾病表达谱逆转的类器官药物筛选研究方案 V4

## 一、研究目标

本项目拟建立一个以**药物诱导转录扰动预测 → 疾病表达谱逆转 → 药物库排序**为核心，同时能够从癌细胞系逐步泛化到患者来源类器官（PDO）的计算药物筛选体系。

最终科学路径固定为：

**公开大规模扰动数据 → 通用扰动模型 → 疾病状态条件化预测 → 疾病表达谱逆转 → 细胞系药效闭环验证 → Cell line→Organoid 机制迁移 → PDO 药物排序。**

项目不以传统的：

`SMILES + Expression → IC50`

作为核心。

真正需要回答的是：

> 对于一个新的疾病样本或 PDO，给定其给药前转录组状态和疾病表达签名，模型能否从数千个候选药物中预测每个药物在该 biological context 中可能造成的转录变化，并判断这些变化是否能够把疾病异常状态推回正常方向，最终把真实有效药物排到候选库前部？

---

# 二、必须首先区分：训练与实际筛药推理是两套输入

这是整个系统的数据逻辑基础。

## 2.1 训练阶段

模型训练阶段学习的是：

> **某种 perturbagen 进入某种 biological context 后，会造成怎样的转录变化。**

典型 chemical training sample：

```text id="281k29"
Untreated / DMSO Context
+
Chemical Perturbagen
+
Drug Mechanism Information
+
Dose / Time
        ↓
Observed drug-induced transcriptional change
```

标签主要来自：

```text id="uw7aq7"
Δ978 landmark genes
```

以及存在真实 whole-transcriptome 数据时的：

```text id="cmg8my"
non-landmark Δ genes
```

Genetic perturbation 的训练样本则是：

```text id="zw7ysa"
Untreated / Control Context
+
CRISPRi / CRISPRa / KO perturbagen
+
Genetic Mechanism Representation
        ↓
Observed transcriptional response
```

---

## 2.2 实际筛药推理阶段

对于一个新的疾病样本或 PDO，真正需要的核心输入是：

### 输入 1：Disease Transcriptome

即给药前的疾病状态表达谱。

例如：

- tumor RNA-seq；
- untreated PDO RNA-seq；
- untreated disease organoid RNA-seq。

它用于回答：

> **这个具体疾病样本当前处于什么 cellular context？**

---

### 输入 2：Disease Signature

即疾病相对于正常/reference 的差异表达信息：

```text id="q34us8"
Gene
log2FoldChange
P value
Adjusted P / FDR
Up / Down
```

它用于回答：

> **这个疾病到底哪些基因/通路出现异常，我们希望药物纠正什么？**

---

### 输入 3：Candidate Drug Library

候选药物库。

药物的：

- UniPert representation；
- chemical structure；
- known target；
- target action；
- STRING network；
- Reactome/GO pathway；

都属于**预先建立的药物知识库**，不是每次筛药要求用户重新提供的信息。

所以最终实际筛药接口应明确写成：

```text id="i38yjl"
Disease Transcriptome
+
Disease Signature
+
Candidate Drug Library
        ↓
Drug Ranking
```

如果用户只有 disease transcriptome 而没有 Disease Signature，则额外提供：

**Normal / Reference transcriptome**

由系统先建立 Disease Signature。

---

# 三、总体科学架构

整个系统由五个相互衔接的知识层构成：

```text id="xmvkte"
                PUBLIC PERTURBATION DATA
                         │
        ┌────────────────┴────────────────┐
        │                                 │
 Chemical Perturbation            Genetic Perturbation
 LINCS / sci-Plex                 LINCS / Perturb-seq
        │                                 │
        └────────────────┬────────────────┘
                         ↓
             UNIFIED PERTURBAGEN SPACE
                    UniPert-G2CP
                         │
                         ↓
               PERTURBAGEN MECHANISM
                         │
               Target / Action / Affinity
                         │
                      STRING
                         │
              Target-proximal Pathway
                         │
                         ↓
                  MECHANISM STATE
                         │
                         ×
                         │
                 BIOLOGICAL CONTEXT
              untreated transcriptome
                         │
                         ↓
             CONTEXTUALIZED PERTURBATION
                         │
        ┌────────────────┼────────────────┐
        ↓                ↓                ↓
     Δ978 Core      Extended Genes      Pathway
        │                │                │
        └────────────────┼────────────────┘
                         ↓
                  DISEASE REVERSAL
                         ↓
                  MOLECULAR RANKING
                         ↓
               CELL-LINE EFFICACY GATE
              PRISM / CTRP / GDSC
                         ↓
             CELL LINE → ORGANOID
                 MECHANISM TRANSFER
                         ↓
                PDO EXTERNAL TEST
```

核心不是简单让：

```text id="k96ljk"
Cell-line embedding
≈
Organoid embedding
```

而是：

> **共享作用机制，条件化最终响应。**

也就是尽量保持：

```text id="gc5kft"
Drug
→ Target
→ Pathway
```

而：

```text id="wfuw7j"
Pathway
→ Gene response
→ Phenotype
```

由具体 biological context 决定。

这也是前期 OrganoPert 方案中已经提出的核心归纳偏置。

---

# 四、为什么这条架构具有前人依据

本项目不是从零发明一个网络，而是将几个已经获得高水平论文支持的能力组合成一个面向疾病逆转和类器官的完整系统。

XPert 已证明可以联合使用 pre-perturbation cellular state、drug representation、dose/time 对 drug-induced transcriptional change进行条件预测，并系统评价 unseen drug、unseen cell 和 multidose/multitime 情况。

MultiDCP 已证明 drug、gene representation、basal cellular context 和 dose 可以共同用于 context-dependent differential gene-expression prediction，并专门评价 novel cell/context。

UniPert-G2CP 则直接证明 genetic 和 chemical perturbagens 可以映射到统一表示空间，并通过 genetic→chemical transfer 提高 chemical phenotype modelling 与泛化。

而三维 organoid pharmacogenomics 已经证明，利用 known drug targets、STRING PPI 和 target-proximal pathways可以构建具有患者药效预测价值的机制型 biomarkers。

因此本项目真正新增的不是这些组件本身，而是：

> **如何把统一 perturbagen representation、显式 Drug–Target–Pathway 知识、context-conditioned perturbation prediction、可靠扩基因和 disease reversal 组合成严格可验证的 Cell line→Organoid 筛药闭环。**

---

# 五、第一核心：978 Landmark Gene Space

978 landmark genes 是整个系统不可替代的最高可信扰动核心。

GSE92742 提供超过 130 万条 L1000 perturbational profiles；Level-2 是直接测量 978
landmark expression 的可选资源。官方定义的 Level-3 经 invariant-set scaling 与
quantile normalization 后同时包含直接测量的 landmark transcripts 和 inferred
non-landmark genes；因此本项目可从 Level-3 的 `gene_info.pr_is_lm=1` 精确提取
978-gene core，并不得将其余 inferred genes 当作真实 whole-transcriptome 标签。

因此必须长期维护：

```text id="l5jyk1"
G_core = 978 landmark genes
```

所有候选药首先必须能够预测：

```text id="jphe6i"
Δ978
```

扩基因成功与否都不能改变这一点。

---

# 六、Drug / Perturbagen Mechanism Encoder

这一部分由两种信息共同构成，但不是两个互不联系的独立模型。

## 6.1 UniPert：Learned Mechanism Space

对于 chemical drug：

```text id="i2ko0s"
Drug
↓
UniPert
↓
Chemical Perturbagen Representation
```

对于 genetic intervention：

```text id="aslmey"
CRISPRi / CRISPRa / KO
↓
UniPert
↓
Genetic Perturbagen Representation
```

两者进入统一空间。

UniPert-G2CP 的核心贡献正是统一 genetic 和 chemical perturbagens，并证明 genetic-to-chemical transfer 可以改善预测。

这对于类器官尤其重要：

```text id="nlpu6f"
大量 cell-line chemical data
+
类器官 genetic perturbation data
        ↓
共享 perturbagen mechanism
```

从而缓解 organoid chemical transcriptomics 严重缺失的问题。

---

# 七、Drug–Target–Pathway：显式机制桥梁

UniPert学习的是隐式 mechanism representation。

同时建立显式：

```text id="6ol049"
Drug
↓
Known Targets
↓
Action / affinity / confidence
↓
STRING PPI
↓
Target-proximal Reactome / GO pathways
```

Nature Communications 的 organoid pharmacogenomics 工作已经采用 known drug targets + STRING + target-proximal pathways，并在患者药物反应及外部药敏数据中进行验证。

所以这一层不是事后解释模块。

它承担：

1. Drug mechanism prior；
2. chemical↔genetic mechanism 对照；
3. Cell line→Organoid 迁移共同坐标；
4. disease pathway rescue；
5. 最终机制解释。

---

# 八、UniPert 和 Drug–Target–Pathway 如何结合

不采用一个绝对硬链：

```text id="q9f1o7"
Drug
→ 必须存在 Target
→ 才能预测
```

因为真实药物存在：

- target annotation 不完整；
- polypharmacology；
- off-target；
- unknown mechanism。

所以正式机制表示为：

```text id="l7no7t"
UniPert learned mechanism
        +
Target/PPI/Pathway structured prior
        ↓
Unified Mechanism State
```

显式 target 信息存在时增强机制约束；

target 缺失时仍可依赖 UniPert/chemical representation进行预测，但：

**Mechanism Confidence 降低。**

算法实现上只使用低容量 projection/gating 进行融合，并通过 ablation 判断 Target–Pathway 信息是否真正增加 OOD 泛化；不预设复杂 KG-GNN 一定有效。

---

# 九、机制知识的公开数据来源

核心机制数据库必须公开可获取。

Open Targets 提供公开 target、drug、disease、target–disease 等数据，可通过下载和 API 获得。

STRING 提供 protein interaction network。

Reactome/GO 提供 pathway/function annotation。

Drug bioactivity 和 target 信息可使用 ChEMBL，再结合 Open Targets 整理：

```text id="30puht"
Drug
Target
Action
Affinity
Evidence confidence
```

因此正式方案不依赖必须商业授权才能取得的完整 proprietary knowledge graph。

---

# 十、Biological Context Encoder

模型必须预测：

```text id="kovxsf"
Perturbagen × Context
→ Response
```

而不是：

```text id="1klp65"
Drug
→ fixed expression signature
```

训练阶段 Context 来源：

- LINCS DMSO/control；
- CCLE/DepMap basal expression；
- sci-Plex untreated cells；
- organoid control samples。

推理阶段 Context 来源：

**Disease Transcriptome。**

---

# 十一、训练 Context 与推理 Context 分布不一致的问题

这是正式方案中的关键风险之一。

训练：

```text id="46cnbp"
LINCS / cell-line control
```

推理：

```text id="res580"
tumor/PDO RNA-seq
```

二者不能直接按原始值进入同一个模型。

所以统一 Context pipeline：

```text id="zx7d8x"
Raw expression
↓
Gene-ID harmonization
↓
978 landmark extraction
↓
dataset/platform-specific normalization
↓
relative expression / rank / bin representation
↓
Context Encoder
```

不尝试把 RNA-seq TPM 人为转换成“假的 LINCS Level-3 数值”。

XPert 已采用 gene-aware context representation 和条件建模，并展示 preclinical→独立数据 fine-tuning 的可行性；PrePR-CT进一步证明 untreated target-cell expression可用于建立 cell-type-specific prior、支持 unseen context/small-data prediction。

---

# 十二、Disease Transcriptome 与 Disease Signature 必须分离

这两个输入不能混为一谈。

## Disease Transcriptome

进入：

```text id="n1a4be"
Context Encoder
```

回答：

> 这个患者/PDO现在是什么状态？

## Disease Signature

进入：

```text id="yfl8zn"
Reversal Engine
```

回答：

> 相对于正常状态，哪里发生了异常？

因此：

```text id="oy2d8w"
Disease Transcriptome
        ↓
Perturbation Prediction
```

而：

```text id="fphzch"
Disease Signature
        ↓
Disease Reversal
```

---

# 十三、Disease Signature 构建

疾病签名不是人为固定成 978、1000 或 2000 genes。

通常首先进行：

```text id="hhlw2b"
Disease
vs
Normal / Reference
↓
Differential Expression
↓
Gene + log2FC + FDR
```

然后按：

- statistical significance；
- effect size；

确定 disease-up / disease-down genes。

经典 cancer-expression reversal 工作就是先比较 TCGA tumor 与 normal 建立疾病签名，再与 LINCS drug signatures 做逆转；该研究的一个具体设置采用 `|log2FC| > 1.5` 和 adjusted P<0.001，但这是具体研究阈值，不应成为所有疾病的固定标准。

所以本项目保存两种 Disease Signature。

---

# 十四、离散 Disease Signature

保存：

```text id="680nks"
Disease-up genes
Disease-down genes
```

用于经典 CMap/GSEA-style reversal。

筛选阈值基于：

```text id="0hx9ik"
FDR
+
|log2FC|
```

但阈值必须在 development dataset 中做 sensitivity analysis 后冻结。

---

# 十五、完整 Ranked Disease Profile

同时保存所有有效测量 gene 的：

```text id="0hl3el"
signed log2FC
```

形成完整 ranked list。

用于：

- GSEA；
- Reactome/GO pathway analysis；
- pathway rescue；
- signature cutoff robustness。

因此即使最终显著 disease genes 只有几百或几千，也不会丢掉其它基因的 pathway-level information。

---

# 十六、个体化 Disease Signature

如果存在：

```text id="kom27w"
Disease PDO
+
Matched Normal PDO
```

优先建立 patient-specific signature。

如果没有 matched normal：

使用：

```text id="2mn0vu"
Same-tissue normal organoid/reference cohort
```

建立 reference distribution。

如果只有单个患者而不能进行标准 cohort DE，不应伪造普通 p-value；应使用适合 single-sample/reference-based dysregulation 的方法。

MultiDCP 已在个体化疾病药物重定位中使用 patient-specific disease-signature 方法进行 reversal screening，为这种思路提供了直接前例。

---

# 十七、缺失基因怎么处理

正式维护四个 gene sets：

```text id="xn5xr2"
G_context_available
G_disease_measured
G_core
G_extended
```

疾病逆转只能使用：

```text id="98au0a"
G_score
=
G_disease_measured
∩
G_drug_reliable
```

不对没有测量的 disease gene 自动补 0。

同时必须输出：

```text id="2f9thy"
Signature Coverage
```

例如：

```text id="0lkx3u"
Disease DEG      3,200
Usable genes     1,460
Coverage         45.6%
```

coverage 太低：

降低筛药置信度。

---

# 十八、Context 输入缺少部分 landmark genes

这与扩基因完全是另一个问题。

若目标 RNA 平台只有：

```text id="relm3x"
950 / 978
```

landmarks，可以采用：

- explicit missingness mask；
- masked context encoder；
- training-time feature masking；

提高鲁棒性。

这些属于本项目的工程 robustness mechanism，需要通过 masking simulation验证。

如果目标数据只覆盖很少 landmark genes，则不能假装 personalized context 可靠，应：

**abstain 或降级到 tissue/reference context。**

---

# 十九、Context OOD 必须显式评估

在每次筛药推理之前，计算：

```text id="bhxma9"
Disease/PDO Context
vs
Training Context Distribution
```

如果 context 严重 OOD：

不能仍输出高可信 individualized drug prediction。

正式报告应标记：

```text id="dedp69"
In-domain
Moderate OOD
Strong OOD
```

Strong OOD 时：

- 降低 confidence；
- 或退回 tissue-level reference prediction；
- 极端情况下拒绝给出 personalized claim。

---

# 二十、核心 Perturbation Model

主模型不从头设计一个巨型网络。

优先基于 **XPert-style context-conditioned perturbation architecture**。

XPert 已证明可以联合建模 pre-perturbation state、drug properties、dose/time，并在 cold-drug、cold-cell 和 multidose/multitime情境中预测 gene-specific perturbation。

本项目在这一基础上增加：

```text id="7hxy8u"
UniPert unified perturbagen representation
+
Drug–Target–Pathway structured prior
```

而不是推倒重建一个新的 virtual-cell foundation model。

---

# 二十一、模型内部结构

推荐保持四个主要模块。

## A. Context Encoder

输入：

```text id="3ktcme"
978 basal expression
+
optional pathway/context features
```

输出：

```text id="sp69t7"
Z_context
```

---

## B. Perturbagen Mechanism Encoder

输入：

```text id="23c5kp"
UniPert representation
+
chemical structural information
+
Target–PPI–Pathway prior
+
annotation mask/confidence
```

输出：

```text id="rc824u"
Z_mechanism
```

对于 CRISPR：

同一个 encoder 使用：

```text id="y5i9ls"
UniPert genetic representation
+
perturbed target
+
perturbation direction
```

---

## C. Context × Mechanism Interaction

学习：

```text id="qavgs2"
Z_context
×
Z_mechanism
×
Dose / Time
→
Z_response
```

这是整个模型中真正需要 context conditioning 的地方。

---

## D. Perturbation Output

Primary output：

```text id="oevxu9"
Δ978
```

这是最高优先级。

---

# 二十二、为什么不直接从一开始预测全转录组

因为 978 是 LINCS 最强实验监督空间，而 non-landmark inferred genes 并不具有同等证据
等级。measurement provenance 与 processing level 必须分开：Level-3/4/5 的更大空间同时
包含经相应处理的直接测量 978 landmark subset 与 inferred non-landmark genes；本项目仅以
精确 landmark subset 作为 `Delta978` core supervision。

所以先保证：

```text id="ul2v71"
Δ978 reliable
```

再扩展。

不能反过来。

---

# 二十三、Reliable Gene Expansion

最终维护：

```text id="ifkmi6"
G_core      = 978
G_extended  = experimentally calibrated extended genes
```

978 不会被 G_extended 替代。

---

# 二十四、扩基因依据

MultiDCP 已经直接证明了 drug、gene representation、cell context、dose共同预测 gene-specific chemical response 的架构可行，其 gene representation来自 STRING 的大规模 gene network。

GPS 则提供另一条重要思路：

> 使用 gene biological features 产生训练 gene universe 外的 candidate predictions，再用独立 whole-transcriptome perturbation data逐基因筛选可信预测。

由于此前 GPS 结果存在复现风险，本项目：

**借方法，不借结果。**

也就是说：

- 不直接使用 GPS checkpoint；
- 不直接使用论文 gene list；
- 不规定最终必须得到某个特定 gene 数。

---

# 二十五、Whole-Transcriptome Chemical Data

第一重要资源：

**sci-Plex / GSE139944。**

它公开了多种细胞系、小分子处理和 single-cell RNA-seq 数据，原始和 processed data均可获取。

它承担：

> non-landmark genes 的真实直接实验监督。

single-cell 数据首先按：

```text id="kajqck"
cell line
×
drug
×
dose
×
replicate
```

形成 pseudobulk/metacell，再作为主监督单位。

---

# 二十六、Independent Whole-Transcriptome Calibration

不能把所有 WTS 数据都拿去训练。

必须留出真正独立 calibration dataset。

推荐：

### PANACEA / GSE186341

公开包含：

- 32 kinase inhibitors；
- 11 cancer cell lines；
- 1,728 RNA-seq profiles；
- DMSO/water controls；
- 两个 replicates。



### DRP006006

公开包含：

- 23 lung cancer cell lines；
- 95 compounds；
- 3,240 RNA-seq datasets；
- 以及多 dose/time 子实验。



至少一个必须完全不参与 gene expansion fitting。

---

# 二十七、Extended Gene Reliability

模型冻结以后，对每个 candidate extended gene 独立评价：

```text id="060imp"
direction accuracy
balanced accuracy
macro-F1
effect-size correlation
replicate reproducibility
unseen-drug consistency
cross-cell consistency
```

只有通过预先定义 reliability criteria 的 genes 才进入：

```text id="zp2ij9"
G_extended
```

所以最终可能得到：

900、1800、3000、4000……

由数据决定。

不是为了“凑几千个”。

---

# 二十八、Pathway 在模型里有三个不同角色

不能统称为“Pathway 模块”。

## 28.1 Mechanism Prior

来自：

```text id="8z49xa"
Drug
→ Target
→ STRING
→ Reactome/GO
```

告诉模型药物最接近哪些 biological programs。

---

## 28.2 Transfer Coordinate

Cell line 和 organoid 不强制具体 gene response一致，但要求：

**相同 mechanism 的 pathway-level biological direction 尽量保持合理一致。**

这与前期方案“共享机制，不强制共享最终表达结果”完全一致。

---

## 28.3 Disease Rescue

疾病完整 transcriptome经过 GSEA/Reactome/GO 等得到：

```text id="bn2iu4"
Disease pathway ↑
Disease pathway ↓
```

再比较 drug-induced pathway state。

Pathway 在这里直接参与筛药。

---

# 二十九、Pathway 不需要再造一个巨大深度网络

正式版本优先采用：

```text id="hayuny"
Predicted reliable Δgenes
↓
Gene-set/pathway aggregation
↓
Predicted pathway change
```

并与：

```text id="45enqa"
Drug target-proximal pathway prior
```

交叉验证。

如果后续证明可学习 pathway head 在严格 OOD 上确实改善结果，再升级。

主方案不预设复杂 pathway neural network 一定有效。

---

# 三十、完整公开数据清单

| 数据层 | 数据集/资源 | 作用 |
|---|---|---|
| Chemical perturbation core | **LINCS GSE92742** | Δ978 主训练；公开超过百万 profiles |
| External LINCS | **GSE70138** | independent LINCS perturbation validation |
| WTS chemical | **sci-Plex GSE139944** | non-landmark direct chemical supervision |
| WTS calibration | **PANACEA GSE186341** | independent extended-gene/pathway validation |
| WTS calibration | **DRP006006** | 23 lines/95 compounds whole-transcriptome external validation |
| Basal context | **CCLE / DepMap** | cell-line untreated context |
| Chemical↔Genetic representation | **UniPert pretrained resources** | unified perturbagen mechanism |
| Drug–Target | **ChEMBL / Open Targets** | target/action/evidence |
| Target–Disease | **Open Targets** | disease mechanism support；公开下载/API |
| PPI | **STRING** | target-proximal network |
| Pathway | **Reactome / GO** | mechanism/pathway space |
| Cell-line phenotype | **PRISM** | 首要 drug-ranking benchmark；4,518 drugs × 578 cell lines resource |
| Cell-line phenotype | **CTRPv2** | second pharmacogenomic benchmark |
| Cell-line phenotype | **GDSC1/2** | cross-study external test；DrEval支持严格 cross-study evaluation |
| Disease expression | **TCGA/GDC / GEO disease cohorts** | disease-vs-normal signature |
| Organoid genetic | **GSE280506** | gastric organoid CRISPRi/a + CROP-seq，processed matrix公开 |
| Organoid genetic | **GSE145308** | intestinal organoid CRISPR + RNA-seq，raw/processed data公开 |
| Organoid external | **GSE167285** | donor-level colon organoid validation |
| Organoid external | **GSE241659** | PTEN intestinal-organoid validation |
| PDO phenotype | **GSE117548** | 16 CRC PDO transcriptomes + >500 compounds，近600万图像 |
| Organoid disease/reference | **GSE64392** | CRC living biobank；20 patients，多数具有 adjacent normal organoids |

原则是：

> **核心方案中的数据必须可以公开获取；如果后续某数据事实无法获得，则不得成为关键依赖。**

---

# 三十一、完整训练阶段

## Phase 0 — Data Harmonization

首先建立统一：

```text id="3yez4f"
Drug ID
Gene ID
Cell/Context ID
Dose
Time
Target
Pathway
Phenotype
```

Drug canonicalization 必须发生在 split 以前：

- canonical SMILES；
- salts；
- stereochemistry；
- aliases；
- duplicate structures。

Transcriptome perturbation优先使用：

```text id="ayz8x3"
Perturbed
-
Matched Control
```

用户已有研究方案同样强调 compound canonicalization 后才划分数据，以及 matched-control 而不是 global-control subtraction。

---

# 三十二、Phase 1 — 978 Chemical Perturbation Core

训练：

```text id="gkd0g3"
LINCS DMSO Context
+
Chemical Perturbagen
+
Dose / Time
+
Mechanism Information
        ↓
Δ978
```

先做好标准 condition：

例如高覆盖的 canonical dose/time。

再扩 multidose/multitime。

主 backbone参考 XPert。

---

# 三十三、Phase 2 — Chemical + Genetic Unified Training

把 chemical 和 genetic perturbagens通过 UniPert统一表示。

训练：

```text id="6bx3nn"
Perturbagen
×
Context
→
Response
```

数据来自：

- LINCS chemical；
- LINCS genetic；
- Perturb-seq 等。

作用是学习：

**perturbagen-level shared biological cause space。**

UniPert-G2CP 已经直接证明 genetic→chemical transfer这一总体策略可行。

---

# 三十四、Phase 3 — Drug–Target–Pathway Mechanism Learning

加入：

```text id="5p1v5t"
Drug
↓
Target
↓
STRING
↓
Pathway
```

但采用软约束。

正式验证必须比较：

```text id="mqmi7i"
Model without mechanism prior
vs
Model + UniPert
vs
Model + Target/Pathway
vs
Model + both
```

只保留严格 OOD 中真正增加价值的 mechanism information。

---

# 三十五、Phase 4 — Reliable Gene Expansion

加入 sci-Plex 等真实 WTS chemical perturbation。

利用 gene-aware modelling 扩展 non-landmark supervision。

然后：

**冻结模型。**

用 PANACEA / DRP006006 做独立 per-gene calibration。

得到：

```text id="fhdkcu"
G_extended
```

---

# 三十六、Phase 5 — Disease Signature Engine

与 perturbation model 独立。

输入：

```text id="axgzsl"
Disease
Normal / Reference
```

输出：

```text id="xx9sii"
Disease-up
Disease-down
log2FC
FDR
full ranked disease profile
disease pathways
```

这一步是标准生物信息学流程，而不是深度模型的一部分。

---

# 三十七、疾病逆转由三个独立通道组成

## S1 — Landmark Reversal

```text id="ez6vkj"
Disease DEG ∩ 978
           ↕
Predicted Δ978
```

最高可信。

---

## S2 — Extended Reversal

```text id="ykq88g"
Disease DEG
∩
G_extended
```

按照每个 gene reliability加权。

---

## S3 — Pathway Rescue

```text id="z73gqv"
Disease pathway state
           ↕
Drug pathway perturbation
```

---

# 三十八、Reversal Score 不重新发明

采用已有 CMap/GSEA/KS-style connectivity思想。

2017 年 Nature Communications 已经使用 tumor-vs-normal disease signatures 和 LINCS landmark signatures建立 RGES，并证明表达逆转与癌症 preclinical drug efficacy存在关系。

所以本项目重点不应该放在创造一个“更复杂 reversal neural network”。

重点应该是：

> **药物 perturbation 是否预测得更准、更 context-specific、更可迁移。**

---

# 三十九、必须先完成 Cell-Line 筛药闭环

这是整个项目最关键的验证顺序。

在类器官之前，必须回答：

> 如果在数据最丰富的 cell-line domain 中，模型都无法用 disease reversal把真实 sensitive compounds排到前面，为什么要相信它可以跨到 PDO？

因此：

```text id="5v0str"
Perturbation Model
↓
Disease Reversal
↓
Cell-line Drug Ranking
↓
PRISM / CTRP / GDSC
```

必须先成功。

---

# 四十、Cell-Line 筛药 Benchmark

对于某 cancer type，例如 CRC：

首先建立 cohort disease signature：

```text id="b8h3dj"
CRC Tumor
vs
Normal Colon
```

然后对于每个 CRC cell line：

```text id="afvn1a"
Cell-line basal expression
+
Candidate Drugs
        ↓
Context-specific perturbation
        ↓
Disease reversal
        ↓
Drug ranking
```

再与真实 PRISM/CTRP/GDSC sensitivity比较。

PRISM 本身公开了 4,518 个药物在 578 个 human cancer cell lines上的 viability profiles，并明确作为公共药物重定位资源。

---

# 四十一、Cell-Line Screening Metrics

不能只算一次总体 Pearson。

按**单个 cell line**评价。

Ranking：

```text id="37b502"
Spearman
Kendall
NDCG@10/20/50
```

Screening：

```text id="83yv63"
Recall@K
HitRate@K
Top-K sensitive-drug enrichment
```

Sensitive/Insensitive classification：

```text id="rc8tyi"
AUROC
AUPRC
```

真正重要的是：

> **真实敏感药是否出现在 Top-K。**

---

# 四十二、必须 Cross-study

例如：

```text id="up4kfw"
PRISM
→ development / calibration

GDSC
→ completely frozen external test
```

然后另一个方向：

```text id="hyjjpf"
CTRP
→ development

PRISM
→ external test
```

不能把不同实验中的 IC50/AUC绝对值强行当成同一个 label。

DrEval 2026 强调随机划分、mean effects 和 study-specific leakage可能造成过于乐观的药敏预测，而 cross-study/application-specific evaluation 对真实泛化非常重要。

---

# 四十三、Cell-Line Go/No-Go

进入类器官之前必须同时满足：

### Gate A

unseen drug/context 的 Δ978 perturbation prediction可靠。

### Gate B

disease reversal能够富集真正 sensitive compounds。

### Gate C

该能力可以跨 PRISM/CTRP/GDSC study保持。

任何一个失败：

**优先修复 cell-line 系统，不进入 organoid transfer。**

---

# 四十四、Cell line→Organoid 的迁移对象

不是完整 expression profile。

正式迁移对象是：

```text id="4kk3lw"
Perturbagen mechanism
+
Drug–Target–Pathway
```

也就是：

```text id="hom5wn"
Cell line
Drug → Target → Pathway → Gene response

                 ↓

            shared mechanism

                 ↓

Organoid
Drug → Target → Pathway → context-specific Gene response
```

这比强制：

```text id="qhi1h6"
cell-line gene response
=
organoid gene response
```

生物学上合理得多。

前期 OrganoPert 文件对此已经明确提出“共享机制，不强制共享最终表达结果”。

---

# 四十五、为什么 UniPert 对 Organoid 特别关键

GSE280506 已公开 human primary gastric organoid 的 CRISPRi、CRISPRa、CROP-seq 以及 cisplatin条件，并提供 processed expression matrix、cell identities 和 guide library。

GSE145308公开 human intestinal organoid CRISPR perturbation + RNA-seq，并包含不同 genetic background / TGFβ context。

因此可以学习：

```text id="r2pkbx"
Genetic Perturbagen
+
Organoid Context
        ↓
Organoid Response
```

通过 UniPert shared perturbagen space：

```text id="id42nb"
Genetic perturbation
        ↕
Shared Mechanism
        ↕
Chemical perturbation
```

从而让 organoid genetic data辅助 chemical perturbation context adaptation。

这是本项目的重要研究假设，但不会直接假定一定有效：

**必须通过 chemical/organoid external evidence检验。**

---

# 四十六、Organoid Adaptation 训练什么

进入 organoid 阶段以后：

冻结或高度限制：

```text id="6qkyb3"
general UniPert representation
general chemical mechanism
978 perturbation knowledge
Drug–Target–Pathway prior
```

主要训练：

```text id="kwmp6r"
Organoid Context Encoder
Mechanism × Context interaction
Pathway→Gene response adaptation
small residual adapter
```

即：

> **类器官负责告诉模型“这个机制在我这里怎样表现”，而不是重新告诉模型“这个药是什么”。**

---

# 四十七、Organoid Validation

## Mechanism-level

GSE167285、GSE241659等 held-out organoid genetic perturbation：

检查：

- response direction；
- pathway consistency；
- unseen donor generalization。

这些数据在前期整理中分别包含 human colon organoid SATB2 KO donor dataset和 intestinal PTEN perturbation，可用于外部验证。

---

## Drug-ranking level

GSE117548：

公开 16 个 CRC PDO transcriptional profiles，并进行了 >500 substances 的高通量处理、接近 600 万张 confocal images，适合作为最终 PDO drug-ranking benchmark。

GSE64392：

公开 CRC living organoid biobank；包含20名患者，且多数同时建立 adjacent-normal organoids，非常适合 tumor/normal context 和 disease-signature研究。

---

# 四十八、Phenotype Calibration

Molecular reversal与真实 efficacy不完全等价。

因此在 cell-line data丰富的阶段可以训练一个低容量 calibrator。

输入只允许：

```text id="u2e4ls"
Landmark reversal
Extended reversal
Pathway rescue
Mechanism support
Chemical OOD
Context OOD
Gene coverage
```

输出：

```text id="ndwkfw"
Sensitivity probability / calibrated rank
```

不允许重新输入：

```text id="t4gjgm"
Raw SMILES + Raw Transcriptome
```

否则整个系统会退化成普通 drug-response predictor。

模型优先：

- logistic/ordinal regression；
- GAM；
- shallow GBDT；
- pairwise ranking。

---

# 四十九、最终必须保留两个 Ranking

## Molecular Reversal Rank

纯疾病逆转结果。

## Phenotype-Calibrated Rank

结合 cell-line/PDO phenotype evidence。

如果 calibrator 在 cross-study 或 PDO 中没有额外价值：

**直接删除 calibrator。**

Molecular Rank必须始终独立存在。

---

# 五十、Prediction Confidence

最终每个药同时输出：

### Gene reliability

多少 reversal evidence 来自：

- 978；
- validated extended genes。

### Chemical OOD

candidate drug距离 training chemical space 有多远。

### Context OOD

disease/PDO距离 training contexts 有多远。

### Mechanism confidence

known target/pathway evidence是否充分。

### Model uncertainty

different folds/seeds/ensembles是否一致。

---

# 五十一、最终推理工作流

用户提供：

```text id="v1f2kl"
Disease Transcriptome
+
Disease Signature
+
Candidate Drug Library
```

系统内部执行：

```text id="bnt40m"
Disease Transcriptome
        ↓
Context preprocessing
        ↓
Z_context
        │
        │
Candidate Drug
        ↓
Precomputed:
UniPert
Target
PPI
Pathway
        ↓
Z_mechanism
        │
        └──────────┐
                   ↓
        Context × Mechanism
                   ↓
           Predicted Δ978
                   +
        Reliable Extended Genes
                   +
          Pathway response
                   │
                   │
Disease Signature ─┤
up/down/log2FC/FDR  │
                   ↓
            Disease Reversal
                   │
         ┌─────────┼──────────┐
         ↓         ↓          ↓
     Landmark   Extended    Pathway
     reversal   reversal     rescue
         │         │          │
         └─────────┼──────────┘
                   ↓
            Molecular Rank
                   ↓
        Phenotype Calibration
                   ↓
              Final Rank
                   ↓
        Mechanistic Evidence
              + Confidence
```

---

# 五十二、整个项目的可靠性阶梯

真正的证据必须按这个顺序建立：

```text id="hwxs80"
LEVEL 1
Drug perturbation prediction
是否真实可靠？
        ↓

LEVEL 2
在 cell lines 中，
disease reversal 是否能够筛药？
        ↓

LEVEL 3
这种能力是否能够 cross-study？
        ↓

LEVEL 4
Drug–Target–Pathway mechanism
是否比直接 expression transfer
具有更稳定的跨 context 特性？
        ↓

LEVEL 5
Genetic→Chemical unified mechanism
是否能帮助 organoid adaptation？
        ↓

LEVEL 6
在 unseen organoid donor 中
机制预测是否成立？
        ↓

LEVEL 7
在真实 PDO screen 中
是否把敏感药排进 Top-K？
```

只有前一级成立才进入下一级。

---

# 五十三、核心消融实验

整个系统虽然集成多个前人工作，但必须明确每一部分是否真的产生价值。

至少比较：

```text id="w8mpc2"
Baseline A
Chemical only

Baseline B
Chemical + Context

Model C
Chemical + Context + UniPert

Model D
Chemical + Context + Target/Pathway

Model E
Chemical + Context
+ UniPert
+ Target/Pathway
```

然后再比较：

```text id="kiz8r6"
978 only
vs
978 + Extended
vs
978 + Pathway
vs
978 + Extended + Pathway
```

最终只保留在：

- unseen drug；
- unseen context；
- cross-study；
- organoid；

真正提高性能的模块。

---

# 五十四、主方案不依赖的东西

正式方案明确不依赖：

- L1000 inferred 9196 genes作为真实标签；
- GPS pretrained results；
- 强制预测 20,000 genes；
- 大型通用 KG-GNN；
- unrestricted whole-gene Transformer；
- adversarial cell-line/organoid强行对齐；
- direct SMILES+RNA→IC50主模型；
- 单纯 SHAP作为机制解释。

---

# 五十五、项目真正的创新贡献

## 创新一：可靠分级 Transcriptome

建立：

```text id="3y86yi"
978 experimentally anchored core
+
independently calibrated extended genes
```

而不是把 inferred whole transcriptome当成同等可信。

---

## 创新二：统一 Chemical/Genetic Perturbagen

利用 UniPert 已经证明的 unified perturbagen representation，让 organoid genetic perturbations 能够为 chemical perturbation提供机制信息。

---

## 创新三：Drug–Target–Pathway 作为 Cell line→Organoid 机制桥

不是迁移完整 expression latent，而迁移：

```text id="x7ojgn"
Drug
→ Target
→ Pathway
```

再由 organoid context决定 downstream genes。

---

## 创新四：Disease-context-specific Perturbation

不是：

```text id="6g47qz"
Drug → fixed signature
```

而是：

```text id="gjgs9b"
Disease Transcriptome
×
Drug Mechanism
→
Disease-context-specific perturbation
```

这一建模方向本身已有 XPert、MultiDCP、PrePR-CT 等工作的直接方法支持。

---

## 创新五：完整 Evidence Ladder

真正形成：

```text id="gezr9q"
Perturbation
↓
Disease reversal
↓
Cell-line efficacy
↓
Cross-study
↓
Organoid mechanism
↓
PDO efficacy
```

而不是只报告一个 transcriptome Pearson。

---

# 五十六、最终成功标准

项目只有在下面四个核心问题得到肯定答案时，才能认为整体成立。

### Q1

在 unseen drug / unseen cell 情况下，是否能够可靠预测真实的 Δ978？

### Q2

使用疾病表达谱逆转以后，是否能够在 PRISM/CTRP/GDSC 中把真正敏感药物富集到 Top-K？

### Q3

Drug–Target–Pathway + unified chemical/genetic mechanism 是否能够把 cell-line 学到的 perturbation knowledge 稳定迁移到 organoid context？

### Q4

对于完全未参与训练的 PDO，是否能够根据：

```text id="6t4udx"
Disease Transcriptome
+
Disease Signature
+
Drug Library
```

把真实有效药物排到候选库前部？

---

# 五十七、最终方案定义

> **本项目以 LINCS/CMap 978 个具有直接大规模实验监督的 landmark genes 作为不可替代的转录扰动核心，在 XPert、MultiDCP 等已经验证的 context-conditioned perturbation modelling 范式基础上学习“Perturbagen × Biological Context → Transcriptional Response”；使用 UniPert-G2CP 将 chemical 与 genetic perturbagens 映射到统一机制空间，从而利用更丰富的 genetic perturbation 数据缓解类器官 chemical perturbation transcriptomics 的数据缺口；进一步结合公开 Drug–Target 数据、STRING PPI 和 Reactome/GO 构造 Drug–Target–Pathway 结构化机制先验，把相对稳定的药物机制而非完整 cell-line expression 作为 Cell line→Organoid 迁移的主要对象；在 978 Core 之上，通过 sci-Plex 等真实 whole-transcriptome perturbation data 与独立 PANACEA/DRP006006 calibration建立可靠扩展基因集合；疾病侧则独立根据 disease-vs-normal/reference 的 log2FC 和统计显著性建立 Disease-up、Disease-down 和完整 ranked disease profile，将 Disease Transcriptome 用于 perturbation context，将 Disease Signature 专门用于 reversal scoring；系统首先必须在 PRISM、CTRP、GDSC 等数据丰富的 cancer cell-line pharmacogenomic resources 上证明转录逆转能够真实富集敏感药物并通过 cross-study validation，然后才进入 GSE280506/GSE145308 等公开类器官 perturbation data 的机制/context adaptation，最后在 GSE117548、GSE64392 等 PDO 资源中检验 unseen drug + unseen organoid 的真实筛药能力，并输出带有 Gene Reliability、Drug→Target→Pathway→Disease Rescue 机制证据和 OOD uncertainty 的最终候选药物排名。**
